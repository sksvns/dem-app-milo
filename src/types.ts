export type Album = {
   groupId: number;
   albumId: number;
   title: string;
}

export type ImageData = {
    albumId: number;
    id: number;
    title: string;
    url: string;
    thumbnailUrl: string;
}